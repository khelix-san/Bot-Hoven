In odrder for the scritp to work you need to install timidity and ffmpeg 
To run the script, the .mid file must be in the same folder of the script and pass it's name (no extension) as input parameter
example:
Python3 converter.py test